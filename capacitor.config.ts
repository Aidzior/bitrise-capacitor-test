import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ay.ionic.capacitor.starter',
  appName: 'host',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
